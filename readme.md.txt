A. my approach to the program was rather simple I mainly followed the algorithms we discussed in class i messed up with the looping on certain abstractions not getting the end of the edge cases.

B. DFS is O(V+E) because you traverse each node exactly once